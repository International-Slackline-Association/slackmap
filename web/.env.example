# Fill the values below and rename this files for .env.development and .env.production
VITE_APP_SLACKMAP_MAPBOX_TOKEN=token for mapbox
# Set to "true" to show the full-page maintenance screen instead of the app
VITE_APP_MAINTENANCE_MODE=false
